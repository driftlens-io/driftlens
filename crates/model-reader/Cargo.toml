[package]
name = "driftlens-model-reader"
description = "Reads data model definitions from source files and bytecode"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
driftlens-core = { path = "../core" }
serde = { workspace = true }
anyhow = { workspace = true }
tracing = { workspace = true }
regex = "1"
walkdir = "2"